namespace InfernumMode.Commands
{
    /*
    class AprilFools : ModCommand
    {
        public override CommandType Type => CommandType.Chat;
        public override string Command => "aprilfools";
        public override string Description => "Toggle an april fools override";
        public override void Action(CommandCaller caller, string input, string[] args)
        {
            Utilities.OverrideAprilFirst = !Utilities.OverrideAprilFirst;
            Main.NewText($"April fools override set to: {Utilities.OverrideAprilFirst}", Color.DarkTurquoise);
        }
    }
    */
}
